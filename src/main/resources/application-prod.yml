spring:
  config:
    activate:
      on-profile: prod

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:validate}

  main:
    allow-circular-references: ${SPRING_ALLOW_CIRCULAR_REFERENCES:false}

logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
